version: "3.3"

services:
  db:
    container_name: DB_diveCursos
    image: postgres:9.5
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: diveCursos
    restart: always
